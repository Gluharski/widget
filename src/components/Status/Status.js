const Status = ({ status }) => {
	console.log(status);

	// check for Not Started || Match Postponed || Match Cancelled || Time to be defined

	// Match Finished => short => FT
	return (
		<>

		</>
	)
}

export default Status;